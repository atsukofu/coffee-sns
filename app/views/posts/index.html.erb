<div class="container">
  <div class="top-head">
    sign in / sign up
  </div>
  <div class="top-index">
    <article class="top-index-post">
      <% @posts.each do |post| %>
        <%= post.user.nickname %>
        <%= post.product %>
        <%= post.stars %>
      <% end %>
    </article>
  </div>
</div>